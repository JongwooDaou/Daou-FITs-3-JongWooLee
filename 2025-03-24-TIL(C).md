# TIL: C
## 학습날짜: 2025년 3월 24일

## Problem:
- 

## Solution:
- 

## What I Learned

### 전처리기

- **전처리기란?**
  - 컴파일 전에 코드에서 미리 처리하는 명령어.
  - `#`으로 시작하며 컴파일러가 실행 전에 동작.

- **종류**
  - `#include` : 헤더 파일 포함
  - `#define` : 매크로 정의
  - `#undef` : 매크로 정의 해제
  - `#ifdef` : 매크로가 정의된 경우에만 컴파일
  - `#ifndef` : 매크로가 정의되지 않은 경우에만 컴파일
  - `#if` : 조건에 따라 컴파일
  - `#elif`, `#else`, `#endif` : 조건 분기
  - `#error` : 조건이 맞지 않으면 컴파일 에러 발생
  - `#pragma` : 컴파일러에 특정 지시
