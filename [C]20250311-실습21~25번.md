```c
void practice22() {
	double d = 100.0;
	double* dpoint = &d;
	printf("d = %.1f\n", d);
	printf("adress of d = %p\n", &d);
	printf("dpoint = %p\n", dpoint);
	printf("*dpoint = %.1f\n", *dpoint);
	printf("size of d = %d\n", sizeof(d));
	printf("size of adress of d = %d\n", sizeof(&d));
	printf("size of dpoint = %d\n", sizeof(dpoint));
	printf("size of *dpoint = %d\n", sizeof(*dpoint));
}

void practice23() {
	int i, * ip = &i;
	int sum = 0, * sump = &sum;
	for (*ip = 1; *ip <= 100; (*ip)++) {
		*sump += *ip;
	}
	printf("sum = %d\n", sum);

}
void practice24() {
	int score = 100;
	int* pscore, ** ppscore, *** pppscore;
	pscore = &score;
	ppscore = &pscore;
	pppscore = &ppscore;
	printf("score = %d\n", score);
	printf("pscore = %d\n", *pscore);
	printf("ppscore = %d\n", **ppscore);
	printf("pppscore = %d\n", ***pppscore);
	printf("=====================\n");
	printf("adress of score = %p\n", &score);
	printf("value of pscore = %p\n", pscore);
	printf("=====================\n");
	printf("adress of pscore = %p\n", &pscore);
	printf("value of ppscore = %p\n", ppscore);
	printf("=====================\n");
	printf("adress of ppscore = %p\n", &ppscore);
	printf("value of ppscore = %p\n", pppscore);


}
void practice25() {
	int score[5] = { 99,89,98,88,85 };
	int* pscore, i, sum = 0;
	pscore = score;
	if (pscore + 2 == score + 2 && pscore + 2 == &score[2])
		printf("두 번째 요소의 주소가 모두 같다\n");
	if (*(pscore + 2) == *(score + 2) && *(pscore + 2) == score[2])
		printf("두 번째 요소의 값이 모두 같다\n");

	for (i = 0; i < 5; i++) {
		sum = sum + *pscore++;
	}
	printf("sum: %d\n", sum);
}
```
