# TIL: C
## 학습날짜: 2025년 3월 10일
## Problem:
- C에서 매크로 함수, 배열, 정렬을 학습하고 직접 구현해보기.
## Solution:
- #define을 사용하여 매크로 함수를 정의하고 활용.
- 1차원 및 2차원 배열을 선언하고 데이터 저장 및 접근.
- 버블 정렬과 퀵 정렬을 사용하여 배열 정렬 구현.

## What I Learned
### 매크로 함수 (Macro Function)
- 매크로 함수란? #define을 사용하여 함수처럼 사용할 수 있는 코드 블록.
- 장점: 실행 속도가 빠름 (컴파일 시 치환됨).
- 단점: 디버깅이 어려울 수 있음.
```c
#include <stdio.h>

#define SQUARE(x) ((x) * (x))  // 매크로 함수 정의

int main() {
    int num = 5;
    printf("SQUARE(%d) = %d\n", num, SQUARE(num));
    return 0;
}
```
결과: SQUARE(5) = 25

### 배열 (Array)
- 같은 타입의 데이터를 연속된 메모리에 저장하는 구조.
- - 1차원 배열: int scores[5] = {90, 85, 78, 88, 95};
  - 2차원 배열: 표 형태의 데이터를 저장할 때 유용함.
```c
#include <stdio.h>

int main() {
    int scores[2][3] = {
        {85, 90, 78},  // 1번 학생의 점수
        {88, 92, 80}   // 2번 학생의 점수
    };

    printf("2번 학생의 영어 점수: %d\n", scores[1][1]); // 92 출력
    return 0;
}
```

### 정렬 (Sorting)
- 버블 정렬 (Bubble Sort)
  - 인접한 두 개의 원소를 비교하면서 swap하는 정렬 방식.
  - 시간 복잡도: O(N²) → 작은 데이터에서 사용 가능.
```c
#include <stdio.h>

void bubbleSort(int arr[], int size) {
    for (int i = 0; i < size - 1; i++) {
        for (int j = 0; j < size - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {  // 오름차순 정렬
                int temp = arr[j];
                arr[j] = arr[j + 1];
                arr[j + 1] = temp;
            }
        }
    }
}

void printArray(int arr[], int size) {
    for (int i = 0; i < size; i++)
        printf("%d ", arr[i]);
    printf("\n");
}

int main() {
    int arr[] = {64, 34, 25, 12, 22, 11, 90};
    int size = sizeof(arr) / sizeof(arr[0]);

    bubbleSort(arr, size);
    printArray(arr, size);

    return 0;
}
```
✅ 결과: 11 12 22 25 34 64 90

