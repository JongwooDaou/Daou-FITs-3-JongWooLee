# TIL: C
## 학습날짜: 2025년 4월 1일

## What I Learned

### JSON
- JSON(JavaScript Object Notation)은 데이터를 구조화하여 저장하거나 전송할 때 사용하는 텍스트 기반의 경량 데이터 형식이다.
- C 언어에서는 JSON을 직접 파싱하기 위해 `cJSON` 등의 라이브러리를 사용할 수 있다.
- 주로 백엔드 시스템과 통신하거나 설정 파일을 읽을 때 사용된다.

### Parsing
- 파싱(parsing)은 데이터를 의미 있는 구조로 해석하는 과정이다.
- JSON 파싱을 통해 문자열 형태의 JSON 데이터를 구조체나 변수로 변환하여 사용할 수 있다.
- C에서의 파싱 작업은 문자열 처리에 익숙해질 수 있는 좋은 연습이다.
