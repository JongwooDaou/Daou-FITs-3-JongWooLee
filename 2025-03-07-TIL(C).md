# TIL: C
## 학습날짜: 2025년 3월 7일

## Problem:
- 익숙하지 않은 C언어의 문법, 사용

## Solution:
- 연습문제를 반복 풀이하며 언어에대하여 익숙해지기

## What I Learned

### 함수
- 표준함수 : 내장함수 또는 시스템 제공 함수라고도 하며 미리 정의되어 있는 프로그래머가
호출하여 사용할 수 있는 함수
- 헤더파일 : C언어에서 미리 만들어 놓은 함수가 정의되어 있는 파일
- 사용자 정의 함수는 사용자가 특정 작업을 수행하기 위해 작성하는 함수로 사용자가 필요에
의해 함수를 정의하고 사용

### 함수 예제 풀
```c
void q1() {
	int gender;
	int ismarried;
	int didmilitaryservice;
	int childnum;
	int point = 0;

	printf("결혼 여부 입력 : 1.기혼 , 2.미혼\n");
	scanf_s("%d", &ismarried);
	printf("성별 입력 : 1.남성, 2.여성\n");
	scanf_s("%d", &gender);
	if (ismarried == 1) {
		point++;

		if (gender == 1) {
			printf("군 복무 여부 입력 : 1.복무함, 2.미복무\n");
			scanf_s("%d", &didmilitaryservice);
			if (didmilitaryservice == 1) {
				point++;
			}
		}
		if (gender == 2) {
			printf("자녀 수 입력 : \n");
			scanf_s("%d", &childnum);
			if (childnum == 1) {
				point++;
			}
			if (childnum == 2) {
				point += 2;
			}
		}
	}

	printf("당신의 점수는 %d입니다.", point);

}
void q2() {
	int n;
	float answer = 0;

	printf("임의의 정수 n을 입력하시오 : ");
	scanf_s("%d", &n);

	for (int i = 1; i <= n; i++) {  
		answer += (i / (i + 1.0));
	}
	printf("합은 %.1f입니다.", answer);
}
void q3() {
	//3. 학생 10명의 퀴즈 점수를 입력받은 후, 전체 평균과 통과자수, 탈락자수를 구해 출력하시오.점수가 70점 이상이면 합격이다.
	int score[10];
	int pass = 0;
	int fail = 0;

	for (int i = 0; i < 10; i++) {
		printf("학생 %d의 점수를 입력하시오 : ", i + 1);
		scanf_s("%d", &score[i]);
		if (score[i] >= 70) {
			pass++;
		}
		else {
			fail++;
		}
	}

	printf("전체 평균은 %.1f입니다.\n", (score[0] + score[1] + score[2] + score[3] + score[4] + score[5] + score[6] + score[7] + score[8] + score[9]) / 10.0);
	printf("합격자수는 %d명입니다.\n", pass);
	printf("탈락자수는 %d명입니다.\n", fail);

}
void q4() {
	//4. 넓이가 100cm2인 사각형 종이를 반으로 접을 때 최초로 1cm2보다 작아지는 횟수를 확인할 수 있도록 넓이 변화를 출력하시오.
	float area = 100;
	int count = 1;

	while (area >= 1) {
		area /= 2;
		printf("%d번째 접힐 때 넓이는 %.2f입니다.\n", count, area);
		count++;
	}

}
void q5() {
	/*5. 특정단의 구구단 출력하기
		키보드로 1~9단 사이의 숫자를 입력받아 그 숫자에 해당되는 구구단을 출력하는 프로그램을
		for문을 이용해 작성하시오*/
	int n;
	printf("1~9단 사이의 숫자를 입력하시오 >> ");
	scanf_s("%d", &n);
	
	for (int i = 1; i <= 9; i++) {
		printf("%d * %d = %d\n", n, i, n * i);
	}
}
void q6() {
	//6. 주사위를 두 개 던졌을 때 합이 7이 되는 경우의 수를 출력하시오

	int count = 0;

	printf("=====================================\n");
	printf("주사위 1\t주사위 2\t합\n");
	printf("=====================================\n");
	for (int i = 1; i <= 6; i++) {
		for (int j = 1; j <= 6; j++) {
			if (i + j == 7) {
				count++;
				printf("%d\t\t%d\t\t%d\n", i, j, i + j);
			}
		}
	}
	printf("=====================================\n");
	printf("주사위를 던져서 합이 7이 되는 경우의 수는 %d입니다.", count);
}
void q7() {
	char arphabet = 122;
	for (int i = 0; i < 26; i++) {
		for (int j = 0; j < 26 - i; j++) {
			printf(" ");
		}
		for (int k = 0; k <= i; k++) {
			printf("%c", arphabet - i + k );
		}
		printf("\n");
		
	}


}
void q9() {
	double p = 100;
	int halflife = 14;
	int day;
	printf("방사능 물질의 초기 양은 100g입니다.\n");
	printf("일정 기간마다 반감기가 일어나는 방사능 물질의 반감기 기간은 14일입니다.\n");
	printf("몇일이 지났는지 입력하시오 : ");
	scanf_s("%d", &day);

	while (day > 0) {
		p /= 2;
		day -= halflife;
	}
	printf("방사능 물질의 양은 %.1fmg입니다.", p);
}
void q10() {
	/*10. 알파벳 소문자를 입력받아 대문자로 바꾸는 프로그램을 작성하시오.
		입력된 문자가 소문자가 아니면 다시 문자를 입력받고 ESC키(키값은 Ox1b)가 눌리면 프로그램을 종료하시오*/

	char ch;
	while (1) {
		printf("알파벳 소문자를 입력하시오 : ");

		ch = _getch();
		if (ch == 27) {
			printf("프로그램을 종료합니다.\n");
			break;
		}
		if (ch >= 97 && ch <= 122) {
			printf("%c의 변환된 대문자는 %c입니다\n", ch, ch - 32);
		}
		else {
			printf("다시 입력해주세요.\n");
		}
	}


	
}
void q22() {
	/*22. 자연수 두 개를 입력받은 후 최대 공약수를 구하는 프로그램을 작성하시오
		힌트
		최대 공약수를 구하는 알고리즘
		1 두수 중 큰수를 x, 작은 수를 y라 한다
		2 y = 0 이면 최대 공약수는 x이며 프로그램을 종료한다
		3 r = x % y
		4 x = y
		5 y = r
		다시 2로 돌아간다
		결과화면
		두 개의 정수를 입력하시오 12 8
		최대공약수는 4입니다*/
	int x, y;
	int try = 1;
	int answer = 0;
	printf("두 개의 자연수를 입력하세요: ");
	scanf_s("%d %d", &x, &y);
	if (x < y) {
		int temp = x;
		x = y;
		y = temp;
	}

	if (y == 0) {
		printf("최대 공약수는 %d입니다.", x);
	}
	while (try <= y) {
		if (x % try == 0 && y % try == 0) {
			answer = try;
		}
		try++;
	}
	printf("최대 공약수는 %d입니다.", answer);

}
```
