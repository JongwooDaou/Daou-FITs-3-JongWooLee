### 26번
```c
void practice26() {
	int sum = 10, score[] = { 99, 80, 91, 78, 85 };
	int* psum, * pscore1, * pscore2;
	psum = &sum;
	pscore1 = score;
	pscore2 = score;

	++pscore2;

	printf("sum: %d\n", sum);
	printf("*psum: %d\n", *psum);
	printf("*psum+1: %d\n", *psum + 1);
	printf("++*psum: %d\n", ++ * psum);
	printf("*(psum+1): %d\n", *(psum + 1));
	printf("++psum: %p\n", ++psum);
	printf("=====================================");
	printf("*pscore: %d\n", *pscore1);
	printf("*pscore1+1: %d\n", *pscore1 + 1);
	printf("*(pscore+1): %d\n", *(pscore1 + 1));
	printf("*pscore1 - *pscore2: %d\n", *pscore1 - *pscore2);
	printf("pscore2: %p\n", pscore2);
	printf("pscore1: %p\n", pscore1);
}
```

### 27번
```c
int max(int, int);
int min(int, int);

void practice27() {
	int(*fpmm)(int, int);
	int num1, num2, flag;

	printf("write two numbers:  ");
	scanf("%d %d", &num1, &num2);
	printf("1. max / 2. min :");
	scanf("%d", &flag);

	if (flag == 1) {
		fpmm = max;
		printf("Max is : %d \n", fpmm(num1, num2));
	}
	else if (flag == 2) {
		fpmm = min;
		printf("Min is : %d \n", fpmm(num1, num2));
	}
	else {
		printf("wrong input");
	}
}
int max(int x, int y) {
	if (x > y)
		return x;
	return y;
}
int min(int x, int y) {
	if (x < y)
		return x;
	return y;
}
```
