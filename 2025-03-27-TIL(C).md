# TIL: C  
## 학습날짜: 2025년 3월 27일

---

##  What I Learned

###  공백 포함 문자열 전달  

`scanf()`는 공백 전까지만 입력을 받기 때문에, 공백이 포함된 문자열을 입력받기 위해서는 `fgets()`를 사용해야 한다.

```c
char str[100];
fgets(str, sizeof(str), stdin);  // 공백 포함 문자열 입력 가능
```

- 주의: `fgets()`는 개행 문자(`\n`)도 함께 읽기 때문에, 필요 시 `str[strcspn(str, "\n")] = '\0';` 로 제거해주어야 한다.

---

###  `%-s` 문자열 길이 고정해서 출력  

`printf()`에서 문자열을 일정 너비로 정렬해서 출력할 수 있다. 여러 항목을 표 형식으로 출력할 때 유용하다.

```c
printf("%-20s", "Hello");
```

- `%-20s`: 문자열을 왼쪽 정렬하고 너비를 20칸으로 고정  
- `%20s`: 문자열을 오른쪽 정렬

- 고정 너비를 통해 정렬이 잘 된 출력 결과를 만들 수 있다.

---
