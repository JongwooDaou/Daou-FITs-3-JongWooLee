# TIL: C
## 학습날짜: 2025년 3월 24일

## What I Learned

### OCI (Oracle Call Interface)
- C언어에서 **Oracle DB와 직접 연결**하고 쿼리를 실행하는 API
- C로 DB와 통신하기 위해 **저수준 라이브러리**를 제공
- SQL*Plus 같은 도구가 내부적으로 사용하는 라이브러리

### OCI 주요 핸들
- `OCIEnv`: 환경 핸들 (OCI 세션 전체 환경 관리)
- `OCIError`: 오류 핸들 (에러 처리 담당)
- `OCIServer`: 서버 핸들 (DB 서버와의 연결)
- `OCISvcCtx`: 서비스 컨텍스트 핸들 (세션 및 트랜잭션 관리)
- `OCISession`: 세션 핸들 (사용자 세션)
- `OCIStmt`: Statement 핸들 (SQL 실행)
- `OCIParam`: 파라미터 핸들 (결과 컬럼 등 메타데이터 접근)

### OCI 프로그래밍 흐름
1. `OCIEnvCreate()` → 환경 핸들 생성
2. `OCIHandleAlloc()` → 서버, 에러, 세션 핸들 생성
3. `OCIServerAttach()` → DB 서버 연결
4. `OCILogon2()` → 세션 시작 (로그인)
5. `OCIStmtPrepare()`, `OCIStmtExecute()` → SQL 실행
6. `OCIHandleFree()` → 핸들 해제 및 리소스 정리
7. `OCILogoff()` → 세션 종료

### 기타
- **OCIBindByPos()**: 입력값 바인딩
- **OCIDefineByPos()**: 출력값(결과값) 바인딩
- **check_error()**: 에러 핸들을 통한 에러 메시지 확인
- **트랜잭션 관리**: 기본적으로 Auto-Commit, 수동으로 트랜잭션도 관리 가능

### 사용 목적
- C로 작성된 애플리케이션에서 **직접 Oracle DB에 접속**하고 CRUD 구현
- 실시간 대량 데이터 처리 및 저수준 DB 연동에 적합
