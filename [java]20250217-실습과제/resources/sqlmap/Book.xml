<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper 
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="BookSearchServlet.MyBook">
    <!-- CDATA 는 SQL query 내부의 특수문자(<>)가 tag로 인식되지 않도록 처리 -->

    <select id="selectBookByTitle" parameterType="String" resultType="Book">
    <![CDATA[
        SELECT bisbn, btitle, bprice, bauthor
        FROM BOOK
        WHERE BTITLE LIKE '%' || #{s} || '%'
        ]]>
    </select>
    <select id="selectBookByISBN" parameterType="String" resultType="Book">
    <![CDATA[
        SELECT bisbn, btitle, bprice, bauthor
        FROM BOOK
        WHERE bisbn =  #{s}
        ]]>
    </select>

    <select id="selectAllHashMap"
            resultType="HashMap">
        <![CDATA[
        SELECT bisbn, btitle, bprice, bauthor
        FROM BOOK
        ]]>
    </select>

</mapper>