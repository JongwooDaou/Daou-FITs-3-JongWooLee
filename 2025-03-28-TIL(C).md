# TIL: C  
## 학습날짜: 2025년 3월 28일

## What I Learned

### Chart.js 활용

- `Chart.js`는 JavaScript 기반의 오픈소스 차트 라이브러리로, 웹에서 다양한 형태의 데이터를 시각화할 수 있음.
- `canvas` 요소를 기반으로 차트를 렌더링하며, `line`, `bar`, `doughnut`, `pie`, `radar`, `bubble` 등 다양한 차트 형태를 지원함.
- JSON 데이터를 업로드하여 파싱한 뒤, `labels`와 `datasets`를 구성하면 동적으로 차트를 그릴 수 있음.
- 옵션을 통해 차트의 스타일, 툴팁, 범례 위치, 애니메이션 등을 자유롭게 설정 가능.
- `responsive: true`로 설정하면 다양한 해상도에 맞춰 자동 조정되며, `scales` 옵션으로 축과 단위 표현을 설정할 수 있음.
- `Chart.update()` 또는 `Chart.destroy()` 등을 통해 동적으로 차트를 갱신하거나 삭제할 수 있음.

### 실습한 주요 기능
- 파일 업로드 후 JSON 데이터를 파싱하여 `line`, `bar`, `doughnut` 차트를 렌더링
- 수익률 데이터를 기반으로 `Monthly Return` 그래프 작성
- 수익률이 음수일 경우 색상을 빨간색으로 표시하는 조건부 스타일링 적용
- `canvas.getContext('2d')`를 통해 DOM 요소와 연결하여 실시간 차트 생성



