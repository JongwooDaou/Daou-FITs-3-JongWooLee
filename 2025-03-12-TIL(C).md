# TIL: C
## 학습날짜: 2025년 3월 12일

## Problem:
- 

## Solution:
- 

## What I Learned

### 이중 포인터 (char**, int**)
- 이중 포인터는 포인터를 가리키는 포인터이며, 2차원 배열이나 동적 할당된 메모리를 다룰 때 유용하게 사용된다.
- int**는 2차원 배열을 동적 할당할 때 활용되며, char**는 문자열 배열을 가리킬 때 사용된다.

### 문자열 배열로 문자열 선언과 문자열 포인터로 문자열 선언
- 문자열 배열 (char arr[][size])
  - 문자열을 2차원 배열로 선언하면 각 문자열이 행 단위로 저장되며, 한 번 할당된 배열 크기는 변경할 수 없다.
예제 (문자열 배열)
```c
char arr[3][20] = {"hello", "world", "C"};
printf("%s\n", arr[1]);  // world 출력
```
  - 특징: 메모리상에서 arr[i]는 char*와 유사하게 동작하지만, arr 전체 크기는 변경할 수 없다.
- 문자열 포인터 (char* arr[])
  - 문자열을 포인터 배열로 선언하면, 각 요소는 문자열을 가리키는 포인터가 된다.
예제 (포인터 배열)
```c
char *arr[] = {"hello", "world", "C"};
printf("%s\n", arr[1]);  // world 출력
```
  - 차이점: 선언 방식	메모리 구조	수정 가능 여부
- char arr[3][20]	스택(고정 크기)	❌ 크기 변경 불가능
- char *arr[]	문자열 리터럴을 가리킴	✅ 수정 가능

### 함수 포인터 (int (*fptr)(int, int))
- 함수의 주소를 저장하는 포인터로, 콜백 함수 구현 및 유연한 함수 호출이 가능하다.
예제 (함수 포인터 선언 및 사용)
```c
#include <stdio.h>

int add(int a, int b) { return a + b; }
int multiply(int a, int b) { return a * b; }

int main() {
    int (*fptr)(int, int);  // ✅ 함수 포인터 선언
    fptr = add;  // ✅ add() 함수 주소 저장
    printf("덧셈: %d\n", fptr(3, 5));  // 8 출력

    fptr = multiply;  // ✅ multiply()로 변경
    printf("곱셈: %d\n", fptr(3, 5));  // 15 출력

    return 0;
}
```
- 활용:
  - 콜백 함수 구현
  - 여러 개의 함수 중 실행할 함수를 런타임에 선택

