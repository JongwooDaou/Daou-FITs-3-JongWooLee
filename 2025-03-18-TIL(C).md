# TIL: C
## 학습날짜: 2025년 3월 18일


## What I Learned

# TIL: C
## 학습날짜: 2025년 3월 18일

## What I Learned

### malloc
- `malloc(size)`는 지정한 크기만큼 메모리를 **할당만 하고 초기화하지 않음**
- 반환값: 성공 시 메모리 주소, 실패 시 `NULL`
- 할당된 메모리에는 **이전에 사용된 쓰레기 값(garbage value)** 이 남아있을 수 있음
- 사용 후 반드시 `free()`로 해제해야 함

### calloc
- `calloc(n, size)`는 `n * size` 만큼의 메모리를 **할당하고 0으로 초기화**
- 모든 메모리가 **0으로 세팅**되어 할당됨
- 배열 초기화나 0으로 초기값이 필요한 경우 유용
- `malloc + memset(0)`과 동일한 효과

### realloc
- `realloc(ptr, new_size)`는 기존 `ptr`로 할당된 메모리를 **새 크기로 확장/축소**
- **기존 값은 유지되고 추가 공간은 초기화되지 않음**
- 새 메모리 공간이 필요하면 **다른 위치에 새로 할당 후 기존 데이터를 복사**
- 실패 시 `NULL` 반환 (기존 메모리는 유지)
- 사용 후도 반드시 `free()` 필요

### 동적할당
- **프로그램 실행 중에 메모리를 할당**하는 것
- 스택 메모리와 다르게 런타임에서 크기를 결정할 수 있음
- `malloc`, `calloc`, `realloc` 모두 **힙(Heap) 메모리 영역**에서 공간을 할당
- 동적할당 후 `free()`로 반드시 해제하지 않으면 **메모리 누수 발생**

