# TIL: C
## 학습날짜: 2025년 3월 17일


## What I Learned

### 구조체
- 여러 개의 서로 다른 데이터 타입을 하나로 묶어 사용하는 사용자 정의 자료형.
- 각 멤버는 각자의 메모리 공간을 따로 가짐.
. 연산자로 멤버에 접근 가능.
```c
struct Student {
    char name[20];
    int age;
    double grade;
};

struct Student s1 = { "John", 20, 3.5 };
printf("%s %d %.1f", s1.name, s1.age, s1.grade);
```
- 독립적인 메모리 공간을 차지.
- 복합적인 데이터를 그룹화할 때 유용.
- 메모리는 모든 멤버의 크기 합 + 패딩(정렬) 만큼 할당됨.


### 공용체
- 여러 멤버가 하나의 메모리 공간을 공유하는 사용자 정의 자료형.
- 구조체와 문법은 유사하지만 모든 멤버가 같은 메모리 공간을 사용.
```c
union Data {
    int i;
    float f;
    char str[20];
};

union Data d;
d.i = 10;
d.f = 3.14;
strcpy(d.str, "hello");
```
- 메모리 절약 (한 번에 하나의 멤버만 사용 가능)
- 멤버를 다양한 타입으로 해석할 때 사용
- 네트워크 프로토콜, 하드웨어 레지스터 등에서 자주 사용

### 열거형
- 관련된 정수 상수들을 묶어서 이름을 붙이는 자료형.
- 가독성을 높이고 코드에 의미를 부여할 때 사용.
```c
enum Day { MON = 1, TUE, WED, THU, FRI, SAT, SUN };

enum Day today = WED;
printf("오늘의 요일 번호는 %d\n", today);  // 출력: 3
```
- 자동으로 0부터 시작하거나 직접 값을 지정 가능
- 상수들을 그룹화하여 코드의 명확성을 높임
- 내부적으로는 int 값으로 처리
