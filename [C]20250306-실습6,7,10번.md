### 실습과제6

```c
// 1번
char gender;
int age;
float height;

printf(" 성별은?( 남자라면 M, 여자라면 F >>");
scanf_s("%c", &gender, 1);
getchar();

printf("나이는? >> ");
scanf_s("%d", &age);
getchar();

printf("키는? >> ");
scanf_s("%d", &height);
getchar();

printf("======================\n");

printf("성별: %c\n", gender);
printf("나이: %3d세\n", age);
printf("키: %.1fcm\n", height);


// 2번
char name[10];

printf("이름을 입력하세요 >> ");
gets(name);

puts(name);

char gender1;
printf("your gender: >> ");
gender1 = getchar();
putchar(gender1);*/

// 3번
int num1, num2, num3;

printf("세 개의 정수를 입력하세요 >> ");
scanf_s("%d %d %d", &num1, &num2, &num3);

printf("num1: %d, num2: %d, num3: %d\n", num1, num2, num3);

int numarr[2];
printf("두 개의 정수를 입력하세요 >> ");
scanf_s("%d %d", &numarr[0], &numarr[1]);

printf("numarr[0]: %d, numarr[1]: %d\n", numarr[0], numarr[1]);
```

### 실습과제 7
엔터를 char로 인식

### 실습과제 10
```c
/*
문) ========================
1. 원의 둘레 구하기
2. 원의 넓이 구하기
3. 구의 부피 구하기
4. 그만두기
======================== 원하는 메뉴는 ? 2
>> 반지름은 ? 7
>> 반지름이 7인 원의 넓이는 153.94*/
int menu;
int radius;
printf("=======================\n");
printf("1. 원의 둘레 구하기\n");
printf("2. 원의 넓이 구하기\n");
printf("3. 구의 부피 구하기\n");
printf("4. 그만두기\n");
printf("=======================\n");
printf("원하는 메뉴는 ? ");
scanf_s("%d", &menu);
switch (menu)
{
case 1:
	printf("원의 둘레 구하기\n");
	printf("반지름은 ? ");
	scanf_s("%d", &radius);
	printf("반지름이 %d인 원의 둘레는 %.2f\n", radius, 2 * 3.14 * radius);

	break;
case 2:
	printf("원의 넓이 구하기\n");
	printf("반지름은 ? ");
	scanf_s("%d", &radius);
	printf("반지름이 %d인 원의 넓이 %.2f\n", radius, 3.14 * radius * radius);
	break;
case 3:
	printf("구의 부피 구하기\n");
	printf("반지름은 ? ");
	scanf_s("%d", &radius);
	printf("반지름이 %d인 구의 부피 %.2f\n", radius, 4 / 3 * 3.14 * radius * radius * radius);
	break;
case 4:
	printf("그만두기\n");
	break;
}
```
```c
int num = 0;
printf("정수를 입력하시오 : ");
scanf_s("%d", &num);
int cnt = 0;

for (int i = 1; i <= num; i++) {
	if (i % 2 == 0) {
		cnt += i;
	}
}
printf("짝수합: %d", cnt);
```
