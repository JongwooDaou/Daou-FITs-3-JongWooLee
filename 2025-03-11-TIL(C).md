# TIL: C
## 학습날짜: 2025년 3월11일

## Problem:
- 

## Solution:
- 

## What I Learned
### 포인터 (Pointer)
- 포인터란?
  - 메모리 주소를 저장하는 변수
  - 변수 자체가 아니라, 변수의 주소값을 가짐
  - int *p; → p는 정수를 가리키는 포인터

- 포인터 선언 및 초기화

```c
int num = 10;
int *p = &num;  // `num`의 주소를 저장하는 포인터
```
  - p는 num의 주소를 저장
  - *p를 통해 num의 값을 참조할 수 있음
- 포인터 연산 
  - p + 1 → 다음 메모리 위치로 이동 
  - *(p + 1) → 다음 변수의 값 접근

### 포인터 예제
- 포인터 선언 및 기본 사용
```c
#include <stdio.h>

int main() {
    int num = 42;
    int *p = &num;  // 포인터 선언 및 초기화

    printf("num의 값: %d\n", num);
    printf("num의 주소: %p\n", &num);
    printf("포인터 p의 값(주소): %p\n", p);
    printf("포인터 p가 가리키는 값: %d\n", *p);  // 역참조

    return 0;
}
```
- 포인터 p를 사용해 num의 값을 가져올 수 있음.

- 포인터를 활용한 값 변경
```c
#include <stdio.h>

int main() {
    int num = 10;
    int *p = &num;

    printf("변경 전 num: %d\n", num);
    
    *p = 20;  // ✅ 포인터를 이용해 num 값 변경
    printf("변경 후 num: %d\n", num);

    return 0;
}
```
- 포인터를 통해 직접 num의 값을 변경할 수 있음.

### 배열과 포인터
```c
#include <stdio.h>

int main() {
    int arr[3] = {10, 20, 30};
    int *p = arr;  // ✅ 배열명 자체가 첫 번째 요소의 주소

    printf("첫 번째 요소: %d\n", *p);
    printf("두 번째 요소: %d\n", *(p + 1));
    printf("세 번째 요소: %d\n", *(p + 2));

    return 0;
}
```
- 배열명 arr는 첫 번째 요소의 주소를 가리킴.
- p + 1로 다음 요소에 접근 가능.

### 포인터를 함수 인자로 전달
```c
#include <stdio.h>

void changeValue(int *p) {
    *p = 99;  // ✅ 포인터를 이용해 원본 값을 변경
}

int main() {
    int num = 42;
    printf("함수 호출 전: %d\n", num);

    changeValue(&num);  // ✅ 주소를 전달하여 함수에서 값 변경
    printf("함수 호출 후: %d\n", num);

    return 0;
}
```
- 포인터를 함수에 전달하면, 원본 값을 직접 변경할 수 있음.
