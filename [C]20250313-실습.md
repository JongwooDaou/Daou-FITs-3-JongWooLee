```c
#define MAX_ROWS 5
#define MAX_COLS 5

// 배열 초기값 넣어주기
void initializeArray(int arr[MAX_ROWS][MAX_COLS], int rows, int cols) {
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            arr[i][j] = i * 10 + j;
        }
    }
}

// 배열 출력하기
void printArray(int arr[MAX_ROWS][MAX_COLS], int rows, int cols) {
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            printf("%d ", arr[i][j]);
        }
        printf("\n");
    }
}
// 입력받은 row의 줄 삭제
void deleteRow(int arr[MAX_ROWS][MAX_COLS], int* rows, int cols, int rowToDelete) {
    if (rowToDelete < 0 || rowToDelete >= *rows) {
        printf("잘못된 행 인덱스입니다.\n");
        return;
    }

    for (int i = rowToDelete; i < *rows - 1; i++) {
        for (int j = 0; j < cols; j++) {
            arr[i][j] = arr[i + 1][j];
        }
    }
    (*rows)--; // 행 감소
}

int main() {
    int rows, cols;
    int arr[MAX_ROWS][MAX_COLS];

    // 행과 열 크기 입력
    printf("행과 열의 수를 입력하세요? ");
    scanf("%d %d", &rows, &cols);

    if (rows > MAX_ROWS || cols > MAX_COLS || rows <= 0 || cols <= 0) {
        printf("입력 범위를 초과하였습니다. (최대 %dx%d)\n", MAX_ROWS, MAX_COLS);
        return 1;
    }

    // 배열 초기화
    printf("배열의 초기값는 자동으로 입력됩니다.\n");
    initializeArray(arr, rows, cols);
    printArray(arr, rows, cols);

    // 삭제할 행 입력
    int rowToDelete;
    printf("삭제하려는 행의 인덱스 번호를 입력하세요? ");
    scanf("%d", &rowToDelete);

    // 행 삭제
    deleteRow(arr, &rows, cols, rowToDelete);

    // 수정된 배열 출력
    printf("수정된 배열 출력\n");
    printArray(arr, rows, cols);

    return 0;
}
```

```c
#define ROWS 3
#define COLS 3

// 배열 원소의 합을 구하는 함수
void sum(int** array) {
    int total = 0;
    for (int i = 0; i < ROWS; i++) {
        for (int j = 0; j < COLS; j++) {
            total += array[i][j];
        }
    }
    printf("배열의 합은 %d\n", total);
}

// 배열 원소의 최댓값을 찾는 함수
void find_max(int** array) {
    int max_val = INT_MIN;
    for (int i = 0; i < ROWS; i++) {
        for (int j = 0; j < COLS; j++) {
            if (array[i][j] > max_val) {
                max_val = array[i][j];
            }
        }
    }
    printf("배열의 최대값은 %d\n", max_val);
}

// 배열 원소의 최솟값을 찾는 함수
void find_min(int** array) {
    int min_val = INT_MAX;
    int* ptr = (int*)array;  

    int count = ROWS * COLS;
    while (count--) {
        if (**array < min_val) {  
            min_val = **array;
        }
        ptr++;  
    }

    printf("배열의 최소값은 %d\n", min_val);
}


// 배열 원소를 모두 제곱하는 함수
void square_elements(int** array) {
    printf("배열의 제곱값은:\n");
    for (int i = 0; i < ROWS; i++) {
        for (int j = 0; j < COLS; j++) {
            printf("%d ", array[i][j] * array[i][j]);
        }
        printf("\n");
    }
}
void q0104() {
    int array[ROWS][COLS] = {
        {0, 1, 2},
        {3, 4, 5},
        {6, 7, 8}
    };

    // 이중 포인터 배열 동적 할당
    int* ptr_array[ROWS];
    for (int i = 0; i < ROWS; i++) {
        ptr_array[i] = array[i];
    }
    int** ptr = ptr_array;

    // 함수 포인터 배열
    void (*operations[])(int**) = { sum, find_max, find_min, square_elements };

    int choice;
    while (1) {
        printf("연산 방법을 선택하기\n(0: 합, 1: 최대값, 2: 최소값, 3: 제곱, 4: 종료): ");
        scanf("%d", &choice);

        if (choice == 4) {
            printf("프로그램을 종료합니다.\n");
            break;
        }

        if (choice < 0 || choice > 3) {
            printf("잘못된 입력입니다. 다시 선택하세요.\n");
            continue;
        }

        operations[choice](ptr);
    }

}
```
