### 4번
```c
//  문자
char e, f;
printf("\n문자를 입력하세요 >>");
printf("\n");
e = getchar();
getchar();
f = getchar();

printf("%c, %c 입력되었음.", e, f);

//  정수

int a,b
printf("정수를 입력하세요 >>");
printf("\n");
scanf_s("%d %d", &a, &b);

printf("%d, %d 입력되었음.", a,b);


//  실수
double c, d;
printf("\n실수를 입력하세요 >>");
printf("\n");
scanf_s("%lf %lf", &c, &d);

printf("%lf, %lf 입력되었음.", c, d);

```

### 5번
```c
// 변수 선언
int std_no, kor, world, math;
double ave;

// 대입문을 사용해 변수 값 저장하기
std_no = 2013901;
kor = 89;
world = 100;
math = 95;

// 평균 계산
ave = (kor + world + math) / 3.0;

// 결과 출력

printf("학번: %d\n", std_no);
printf("국어: %d\n", kor);	
printf("영어: %d\n", world);
printf("수학: %d\n", math);
printf("평균: %.2lf\n", ave);
```
