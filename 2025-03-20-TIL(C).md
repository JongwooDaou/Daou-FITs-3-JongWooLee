# TIL: C
## 학습날짜: 2025년 3월 20일

## What I Learned

### 파일 입출력
- 파일을 열고 닫는 방법 (fopen, fclose)
- 파일 읽기/쓰기 (fread, fwrite, fscanf, fprintf, fgets, fputs)
- 파일 포인터 이동 (fseek, rewind)
- 파일 존재 여부 및 에러 처리 (파일 열기 실패 시 perror 사용)

### 파일 읽기/쓰기 (fread, fwrite, fscanf, fprintf, fgets, fputs)
- **fread()**: 이진(binary) 파일에서 데이터를 읽을 때 사용. 주로 구조체나 raw 데이터를 읽을 때 사용.
  - `fread(포인터, 크기, 개수, 파일포인터)`
  - 예) 구조체 1개를 이진 파일에서 읽기
- **fwrite()**: 이진 파일에 데이터를 쓸 때 사용. 주로 구조체를 한 번에 통째로 저장할 때 사용.
  - `fwrite(포인터, 크기, 개수, 파일포인터)`
- **fscanf()**: 텍스트 파일에서 형식을 지정하여 데이터를 읽을 때 사용.
  - 예) `fscanf(fp, "%d %s", &id, name);`
- **fprintf()**: 텍스트 파일에 형식을 지정하여 데이터를 쓸 때 사용.
  - 예) `fprintf(fp, "%d %s\n", id, name);`
- **fgets()**: 텍스트 파일에서 한 줄(또는 버퍼 크기만큼)을 읽어올 때 사용.
  - 줄 단위로 읽을 때 안전하게 사용
- **fputs()**: 텍스트 파일에 문자열을 쓸 때 사용.
  - 자동 개행이 없음 (필요하면 `\n` 추가)

### 파일 포인터 이동 (fseek, rewind)
- **fseek()**: 파일 포인터를 원하는 위치로 이동할 때 사용
  - `fseek(파일포인터, offset, 기준점);`
  - 기준점: `SEEK_SET`(파일의 처음), `SEEK_CUR`(현재 위치), `SEEK_END`(파일 끝)
  - 이진 파일을 읽거나 쓸 때 원하는 위치로 이동할 때 자주 사용
- **rewind()**: 파일 포인터를 파일의 처음으로 이동시킴 (fseek(fp, 0, SEEK_SET)과 동일)
  - 주로 파일 처음부터 다시 읽고 싶을 때 사용


### txt 파일 입출력
- 텍스트 파일로 데이터를 저장 (fprintf, fscanf, fgets)
- 줄 단위로 데이터를 읽고 쓰는 방법
- 텍스트 파일의 기본 포맷 이해

### binary 파일 입출력
- 구조체를 이진 파일로 저장 (fwrite)
- 이진 파일에서 구조체 읽기 (fread)
- 텍스트 파일과 달리 사람이 읽을 수 없는 데이터 포맷

### CSV 파일 입출력
- CSV 형식의 파일로 데이터 출력 (열, 행 구분)
- 쉼표(,)로 구분된 포맷 작성 (fprintf)
- 텍스트 파일로 저장된 CSV를 Excel과 같은 도구에서 읽을 수 있음
