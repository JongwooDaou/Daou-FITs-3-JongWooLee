# TIL: C

## 학습날짜: 2025년 3월 13일

## 목표:
- 함수 포인터 배열에서 서로 다른 반환형을 가지는 함수를 관리하는 방법
- 2차원 배열을 이중 포인터(int**)로 다루면서 순회하는 방법
- while 문을 활용하여 포인터를 이동하며 배열을 탐색하는 방법
- 함수 포인터 배열에서 반환형이 다른 함수를 관리하는 방법


## What I Learned

### 함수 포인터 배열과 반환형 관리
- 함수 포인터 배열은 같은 반환형을 가지는 함수만 저장할 수 있음.
- void 함수와 int 함수를 하나의 배열에서 관리하려면 구조체를 사용해야 함.
```c
typedef struct {
    void (*void_func)(int**, int, int);
    int (*int_func)(int**, int, int);
} Operation;
```
구조체를 활용하면 void 반환 함수와 int 반환 함수를 같은 배열에서 관리할 수 있음.


### 이중 포인터(int**)와 포인터 이동
- int**를 직접 이동하면 행 단위로 이동하기 때문에 열 단위 이동이 불가능함.
- 2차원 배열을 순회할 때 1차원처럼 처리하기 위해 int* ptr = (int*)array; 변환이 필요함.
- 잘못된 예제:
```c
void find_min(int** array) {
    while (count--) {
        ((int*)array)++;  // 🚨 잘못된 방식
    }
}
```
올바른 방식:
```c
void find_min(int** array) {
    int* ptr = (int*)array;
    while (count--) {
        ptr++;  // ✅ `int` 단위로 이동
    }
}
```

### 포인터 기반 최적화 (while 문)
for 문보다 while 문 + 포인터 연산이 더 빠를 수 있음.
int*를 활용한 직접적인 순회가 메모리 캐시 효율성을 높일 가능성이 있음.
ptr++을 사용하면 연속된 메모리를 순차적으로 접근하므로 빠름.
