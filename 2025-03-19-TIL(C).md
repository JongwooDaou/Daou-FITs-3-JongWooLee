# TIL: C
## 학업날짜: 2025년 3월 19일

## What I Learned

### 구조체 배열
- 동일한 구조체를 여러 개 저장할 때 사용.
- 구조체 배열은 메모리 상에서 연속된 공간에 할당됨.
- 예시:
  ```c
  typedef struct {
      char name[20];
      int age;
  } Person;

  Person people[5];
  ```

### 비트 필드
- 구조체 내에서 비트를 단위로 멤버를 관리하는 문법.
- 메모리를 절약하고, 플래그성 데이터를 표현할 때 유용.
- 기본 자료형의 align(정렬 규칙)을 따라음.
- 예시:
  ```c
  struct {
      unsigned int a : 1;
      unsigned int b : 1;
      unsigned int c : 3;
  };
  ```

### 비트 연산
- `&`, `|`, `^`, `~`, `<<`, `>>` 같은 연산자로 비트를 직접 조작.
- 플래그 설정, 클리어, 토글 등에 사용.
- 예시:
  ```c
  flags |= 0x01; // 틀인 비트 ON
  flags &= ~0x01; // 틀인 비트 OFF
  flags ^= 0x01; // 틀인 비트 TOGGLE
  ```

### 파일
- `fopen()`, `fgets()`, `fclose()` 등을 사용한 파일 입출력.
- 파일 끝(EOF)에서 `fgets()`는 NULL 반환.
- 파일 입출력 시 `feof()`, `ferror()`로 상황 체크 가능.
- 예시:
  ```c
  FILE* fp = fopen("data.txt", "r");
  if (fp) {
      while (fgets(buffer, sizeof(buffer), fp)) {
          // 읽은 줄 처리
      }
      fclose(fp);
  }
  ```

