# TIL: C
## 학습날짜: 2025년 3월 14일

## Problem:
- 구조체를 사용하여 여러 데이터를 하나로 묶어 관리하는 방법을 이해하고 싶었음.

## Solution:
- struct 키워드를 사용하여 구조체를 선언하고, 다양한 타입의 데이터를 하나로 묶어 관리하는 법을 학습함.
- 구조체 배열을 선언하여 다수의 데이터를 효율적으로 처리하는 연습을 했음.
- 구조체 내에서 char 배열, int, float 등을 혼합하여 사용하고, fgets(), scanf()로 입력받는 실습을 진행함.

## What I Learned

### 구조체
- 구조체(struct)는 다양한 타입의 데이터를 하나의 사용자 정의 자료형으로 묶어주는 기능.

형식:
```c
struct 구조체명 {
    데이터타입 멤버변수1;
    데이터타입 멤버변수2;
    ...
};
```

예시:

```c
struct student {
    char name[20];
    char student_no[12];
    int sub1;
    int sub2;
    int sub3;
};
```
구조체 사용법

```c
struct student s1;
strcpy(s1.name, "John");
s1.sub1 = 85;
s1.sub2 = 90;
s1.sub3 = 88;
```
구조체 배열

```c
struct student students[100];  // 최대 100명의 학생 저장
```

구조체 입력 예시
```c
for (int i = 0; i < num_students; i++) {
    printf("이름: ");
    fgets(students[i].name, sizeof(students[i].name), stdin);
    printf("과목 1 성적: ");
    scanf("%d", &students[i].sub1);
    getchar(); // 개행 제거
}
```

구조체와 함수

```c
void print_student(struct student s) {
    printf("이름: %s, 평균: %.2f\n", s.name, (s.sub1 + s.sub2 + s.sub3) / 3.0);
}
```
- 구조체를 사용하면 연관된 데이터(이름, 학번, 점수 등)를 하나의 단위로 묶어 관리할 수 있음.
- 구조체 배열을 사용하면 여러 개의 데이터를 효율적으로 저장 및 순회 가능.
- 구조체와 함수를 조합하면 코드의 재사용성과 가독성이 좋아짐.
